datasource db {
  provider = "postgresql"
  url = env("POSTGRES_URL") 
}

generator client {
  provider = "prisma-client-js"
}

model Auth {
  uid         String     @id @default(dbgenerated()) @unique
  email       String     @unique
  password    String
}
